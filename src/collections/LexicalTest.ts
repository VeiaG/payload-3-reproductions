import { CollectionConfig } from "payload";

export const LexicalTest:CollectionConfig = {
    slug: 'lexical-test',
    fields:[
        {
            name:'content',
            type:'richText',
        }
    ]
}